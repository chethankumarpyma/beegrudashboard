import React from 'react';
import {  Button, Row, Col, Select, Input, Checkbox } from 'antd';

function Tasks() {
	const layout = {
		labelCol   : { span: 6 },
		wrapperCol : { span: 16 }
	};
	const tailLayout = {
		wrapperCol : { offset: 6, span: 16 }
	};

	return (
		<Row justify="center">
		</Row>
	);
}

export default Tasks;
